import type {TextStyle, ViewStyle} from 'react-native';

export type TransactionListStyles = {
  container: ViewStyle;
  containerList: ViewStyle;
  option: ViewStyle;
  selectedIndicator: TextStyle;
};
